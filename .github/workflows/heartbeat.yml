name: heartbeat-cron

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Call heartbeat endpoint
        run: |
          curl -X GET https://casaindie.vercel.app/api/heartbeat-cron